//创建一个整形变量toes，并将toes设置为10.程序中计算toes的两倍和toes的平方。打印三个数值，并分别表述以示区分。
#include <stdio.h>
int main(void)
{
    int toes = 10;
    printf("toes的值为%d,toes的两倍为%d,toes的平方为%d\n",toes,toes*2,toes*toes);

    return 0;
}